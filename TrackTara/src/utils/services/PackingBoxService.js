export const getAllPackingBoxes = async () => {
  throw new Error('PackingBoxService: підключіть API коробок або мок');
};

export const createPackingBox = async () => {
  throw new Error('PackingBoxService: підключіть API коробок або мок');
};

export const deletePackingBox = async () => {
  throw new Error('PackingBoxService: підключіть API коробок або мок');
};

export const addToPackingBox = async () => {
  throw new Error('PackingBoxService: addToPackingBox — реалізуйте на бекенді');
};

export const transferPackingBoxContent = async () => {
  throw new Error('PackingBoxService: transferPackingBoxContent — реалізуйте на бекенді');
};
