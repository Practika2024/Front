# ДОДАТКИ

**до дипломної роботи з теми веб-системи підтримки складських операцій (клієнтська частина TrackTara)**

---

## Перелік додатків

| Додаток | Найменування                                              | Стор. |
|---------|------------------------------------------------------------|-------|
| А       | Скріншоти інтерфейсу                                       | …     |
| Б       | UML- та ER-діаграми                                        | …     |
| В       | Специфікація функціональних вимог                          | …     |
| Г       | Таблиця тест-кейсів                                        | …     |
| Д       | Матриця «вимога → тест»; ключові лістинги коду             | …     |
| Е       | Глосарій термінів                                          | …     |

*(Нумерацію сторінок проставити після верстки документа.)*

---

## Додаток А  
### Скріншоти інтерфейсу

Додаток містить екранні знімки основних екранів клієнтської частини системи TrackTara для ілюстрації користувацького інтерфейсу та ключових сценаріїв діяльності оператора, адміністратора та менеджера продажів у режимі демонстрації (за наявності — із використанням мок-API).

---

**Рис. Дод. А.1 — Екран входу в систему**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: форма логіну (email, пароль), посилання на реєстрацію, блок входу через Google за наявності налаштувань.]*

На екрані представлено точку входу користувача: локальна автентифікація та (за конфігурацією) зовнішній вхід через Google OAuth.

---

**Рис. Дод. А.2 — Екран реєстрації**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: форма реєстрації з полями ПІБ, email, пароль.]*

Екран призначено для створення нового облікового запису; після успішної реєстрації користувач перенаправляється відповідно до налаштувань ролей.

---

**Рис. Дод. А.3 — Стартова панель (Warehouse Hub) для складських ролей**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: головна панель із картками/посиланнями на видачу у візок, пакування, траси тощо.]*

Ілюструється точка входу для оператора та адміністратора до основних складських функцій після автентифікації.

---

**Рис. Дод. А.4 — Перенаправлення або окремий екран для користувача з роллю «Guest»**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: сторінка «Доступ ще не налаштовано» / очікування призначення ролі адміністратором.]*

Демонструється обмежений доступ нового користувача до функціоналу до призначення робочої ролі.

---

**Рис. Дод. А.5 — Екран видачі товару у візок (HomePage)**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: список замовлень по сектору та модальний діалог покрокового виймання (номер візка, код продукту, код контейнера, кількість).]*

Наведено ключовий операційний сценарій видачі на лінії складу з використанням замовлення та позицій рядка.

---

**Рис. Дод. А.6 — Екран пакування в коробки**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: поля траси та номера візка, перевірка коду товару/тари, блок «Коробка», таблиця створених коробок.]*

Відображено робочий простір пакувальника з підтвердженням позиції та додаванням до коробки.

---

**Рис. Дод. А.7 — Перегляд товарів у візку на екрані пакування**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: розгорнутий блок або модальне вікно перегляду вмісту візка за номером.]*

Ілюструється перевірка відповідності візка замовленню перед пакуванням.

---

**Рис. Дод. А.8 — Екран створення замовлення**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: вибір сектора, клієнта/траси, таблиця додавання позицій з контейнерів, модальне вікно кількості.]*

Показано формування замовлення з урахуванням наявності товару в тарі обраного сектора.

---

**Рис. Дод. А.9 — Панель продажів / робота з клієнтами та трассами**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: Sales Dashboard або сторінка клієнтів і трасс залежно від реалізації.]*

Демонструється функціонал для ролі менеджера продажів або адміністратора з доступом до відповідних розділів.

---

**Рис. Дод. А.10 — Реєстр тари (список контейнерів)**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: таблиця тар із можливістю фільтрації та переходу до створення/редагування/деталей.]*

Наведено інтерфейс обліку контейнерів на складі.

---

**Рис. Дод. А.11 — Форма створення або редагування контейнера**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: форма параметрів контейнера (тип, сектор, код тощо за реалізацією).]*

Ілюструється адміністративно-операційне ведення даних про тару.

---

**Рис. Дод. А.12 — Екран реєстру нестач (для адміністратора)**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: список записів реєстру нестач та елементи керування переглядом.]*

Відображено модуль обліку нестач для подальшого аналізу.

---

**Рис. Дод. А.13 — Додавання запису в реєстр нестач з екрану видачі**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: модальне вікно з полем кількості та причини під час роботи оператора.]*

Показано зв’язок операційної діяльності з обліком нестач.

---

**Рис. Дод. А.14 — Розділ користувачів та призначення ролей (адміністратор)**

*[МІСЦЕ ДЛЯ СКРІНШОТУ: таблиця користувачів і дія зміни ролей / скидання пароля за наявності.]*

Демонструється управління доступом до функцій системи.

---

*(За потреби обсягу додатка А додати Рис. Дод. А.15–А.16: профіль користувача, реєстр візків, типи контейнерів, сторінка продуктів тощо.)*

---

## Додаток Б  
### UML- та ER-діаграми

У додатку наведено узагальнені моделі для опису архітектури та даних. Конкретні діаграми підлягають експорту з інструменту моделювання (PlantUML, Draw.io, Enterprise Architect тощо) та вставленню замість плейсхолдерів.

---

**Рис. Дод. Б.1 — Діаграмa варіантів використання (UML)**

*[МІСЦЕ ДЛЯ РИСУНКУ UML: актори Operator, Administrator, SalesManager, Guest; зведені прецеденти — авторизація, видача у візок, пакування, створення замовлення, ведення тари, реєстр нестач, управління користувачами.]*

Діаграмa узагальнює межі системи та основні групи можливостей для різних ролей.

---

**Рис. Дод. Б.2 — Діаграмa компонентів клієнтської частини**

*[МІСЦЕ ДЛЯ РИСУНКУ UML: шари Presentation (сторінки React), State (Redux), Services (HTTP/mock factory), Infrastructure (локальне сховище).]*

Показано логічну декомпозицію SPA та точку перемикання між мок-сервісами та реальним API.

---

**Рис. Дод. Б.3 — Діаграмa послідовностей: локальний вхід**

*[МІСЦЕ ДЛЯ РИСУНКУ UML: учасники Користувач, Форма логіну, AuthService, Сервер облікових записів (за наявності), Redux, localStorage; повідомлення signIn → JWT → збереження токена → оновлення стану.]*

Описано типовий ланцюг подій автентифікації при роботі з бекендом або моками залежно від конфігурації.

---

**Рис. Дод. Б.4 — Діаграмa послідовностей: оновлення маркера доступу при відповіді 401**

*[МІСЦЕ ДЛЯ РИСУНКУ UML: HttpClient interceptor, token refresh, повтор запиту або вихід із системи.]*

Ілюструється механізм підтримки сесії на клієнті при роботі з REST API.

---

**Рис. Дод. Б.5 — ER-діаграмa (перспектива серверної бази даних)**

*[МІСЦЕ ДЛЯ ER-ДІАГРАМИ: сутності Користувач, Роль, Замовлення, Рядок замовлення, Контейнер, Продукт, Візок/видача, Коробка пакування, Реєстр нестач — зв’язки 1:N де доречно; примітка, що у демо частина даних може зберігатися в пам’яті моків.]*

Модель відображає цільовий напрям зберігання даних після повної інтеграції з сервером і БД.

---

**Рис. Дод. Б.6 — Узагальнена діаграмa розгортання**

*[МІСЦЕ ДЛЯ РИСУНКУ: браузер клієнта, статичний хостинг або сервер розробки Vite, API-сервер, (за наявності) хмарний OAuth-провайдер Google.]*

Наведено орієнтовну топологію середовища виконання.

---

## Додаток В  
### Специфікація функціональних вимог

Таблиця В.1 містить узагальнений перелік функціональних вимог до клієнтської частини TrackTara. Ідентифікатори В.x використовуються у додатку Д для трасування тестів.

**Таблиця В.1 — Специфікація функціональних вимог**

| ID   | Назва вимоги | Опис вимоги | Актори | Пріоритет | Примітки |
|------|----------------|-------------|--------|-----------|----------|
| В.1 | Авторизація за email і паролем | Користувач проходить автентифікацію й отримує сесію (JWT або еквівалент) | Усі ролі з обліковим записом | Високий | Залежить від API |
| В.2 | Вхід через Google OAuth | Зовнішній вхід через Google Identity Services та обмін credential із бекендом | Усі за налаштуванням | Середній | Потребує налаштування GCP та бекенду |
| В.3 | Обмеження доступу за ролями | Доступ до маршрутів відповідає ролям користувача | Усі | Високий | RBAC на клієнті доповнює серверну перевірку |
| В.4 | Роль Guest до призначення прав | Новий користувач бачить екран очікування без робочих модулів | Guest | Високий | У моках — за політикою реєстрації |
| В.5 | Стартова панель складу | Перехід до ключових складських модулів з одного екрану | Operator, Administrator | Високий | Warehouse Hub |
| В.6 | Видача товару у візок | Покрокове виймання позиції замовлення з урахуванням візка та сектора | Operator, Administrator | Високий | HomePage / pick |
| В.7 | Реєстрація нестач під час видачі | Оператор може зафіксувати нестачу з модального вікна | Operator, Administrator | Середній | Інтеграція з реєстром |
| В.8 | Пакування в коробки | Перевірка позиції за візком і кодом, створення коробки, додавання рядків | Operator, Administrator | Високий | PackingStationPage |
| В.9 | Переклад між коробками | Переміщення рядка вмісту між коробками одного замовлення | Operator, Administrator | Середній | Модальне вікно перекладу |
| В.10 | Створення замовлення | Формування замовлення з сектора з позиціями з тар | Administrator, SalesManager | Високий | CreateOrder |
| В.11 | Облік контейнерів (тара) | Перегляд, створення, редагування, деталі контейнера | Operator (за маршрутами), Administrator | Високий | Маршрути /tare |
| В.12 | Управління продуктами | Перегляд каталогу та операції за правами доступу | Operator, Administrator, SalesManager | Високий | Залежить від маршрутів |
| В.13 | Панель продажів та клієнти | Доступ менеджера продажів до продажних екранів | SalesManager, Administrator | Середній | /sales |
| В.14 | Управління користувачами | Перегляд користувачів і зміна ролей | Administrator | Високий | UsersPage |
| В.15 | Реєстр нестач (перегляд) | Перегляд накопичених записів для аналізу | Administrator | Середній | /brakimag |
| В.16 | Реєстр візків | Облік візків за наявності модуля | Administrator | Низький | /carts/registry |
| В.17 | Профіль користувача | Перегляд/редагування даних профілю за реалізацією | Авторизовані | Середній | /profile |
| В.18 | Збереження стану між сеансами | Відновлення обраних фільтрів та частини стану після перезавантаження | Авторизовані | Середній | redux-persist |
| В.19 | Перемикання mock / real API | Можливість демонстрації без бекенду та підключення до API через змінні оточення | Розробник, демо | Середній | VITE_USE_MOCK_API |
| В.20 | Повідомлення про помилки мережі та валідації | Користувач отримує зрозумілі повідомлення при збоях запитів і помилках форм | Усі | Високий | Toast / Alert |
| В.21 | Адаптивна навігація | Навігація з бокової панелі та мобільного меню для пристроїв з різною шириною екрана | Авторизовані | Середній | Layout |
| В.22 | Захист від несанкціонованого переходу | Неавторизований користувач перенаправляється на логін | Усі | Високий | ProtectedRoute |
| В.23 | Вихід із системи | Очищення токенів і скидання стану авторизації | Авторизовані | Високий | logout |
| В.24 | Синхронізація сесії між вкладками | Узгодження стану після логіну/логауту/оновлення токена в іншій вкладці | Авторизовані | Низький | storage events (за реалізацією) |

---

## Додаток Г  
### Таблиця тест-кейсів

**Таблиця Г.1 — Набір тест-кейсів для перевірки основних функцій**

| № | Модуль | Передумови | Кроки | Очікуваний результат | Статус |
|---|--------|------------|-------|----------------------|--------|
| 1 | Логін | Користувач існує в системі / моках | Відкрити /login → ввести валідні email і пароль → Підтвердити | Перенаправлення на головну або на екран очікування ролі залежно від облікового запису | |
| 2 | Логін | Невірий пароль | Ввести невірий пароль | Повідомлення про помилку, сесія не створена | |
| 3 | Реєстрація | Email вільний | Заповнити форму реєстрації валідними даними | Обліковий запис створено, виконано вхід або перенаправлення згідно політики | |
| 4 | Google-логін | Налаштовано CLIENT_ID та origins | Натиснути кнопку Google, пройти автентифікацію Google | Успішний обмін credential, сесія відкривається при валідній відповіді бекенду | |
| 5 | RBAC | Обліковий запис з роллю Guest | Увійти в систему | Відображається сторінка очікування призначення ролі без бокового меню модулів | |
| 6 | RBAC | Обліковий запис Operator | Увійти | Доступ до видачі, пакування, тари відповідно до маршрутів | |
| 7 | RBAC | Спроба відкрити URL без прав | Увійти як роль без доступу до маршруту, ввести адресу вручну | Повідомлення про відсутність доступу або перенаправлення | |
| 8 | Видача у візок | Є незавершене замовлення в секторі | Обрати сектор, відкрити діалог видачі, пройти кроки візка/кодів/кількості | Операція зафіксована, оновлено списки | |
| 9 | Видача | Невірий код контейнера | Ввести код, що не відповідає рядку | Повідомлення про помилку, операція не виконана | |
| 10 | Нестачі | Відкритий діалог видачі по рядку | Відкрити модальне вікно нестачі, ввести кількість і причину | Запис з’являється у відповідному реєстрі (за API) | |
| 11 | Пакування | Візок і замовлення містять цільову позицію | Обрати трасу, номер візка, код товару/тари → Перевірити | Відображено клієнта, замовлення, залишок до пакування | |
| 12 | Пакування | Створено коробку | Створити коробку для клієнта, додати кількість у швидкому блоці | Рядок з’явився у вмісті коробки | |
| 13 | Пакування | Дві коробки одного замовлення | Перекласти рядок через кнопку «Перекласти» | Кількість зменшилась у джерелі та з’явилась у цілі | |
| 14 | Замовлення | У секторі є наповнені контейнери | Обрати сектор, додати позиції з модалкою кількості, зберегти | Замовлення створено, доступне для видачі | |
| 15 | Тара | Роль Operator | Відкрити список контейнерів, створити новий запис | Контейнер з’являється у таблиці | |
| 16 | Тара | Контейнер існує | Відкрити редагування, змінити поля, зберегти | Дані оновлені | |
| 17 | Користувачі | Роль Administrator | Відкрити список, змінити роль користувача | Роль змінено у інтерфейсі (перевірити повторним входом за необхідності) | |
| 18 | Вихід | Користувач авторизований | Натиснути Вихід | Токени очищено, доступ до захищених сторінок закрито | |
| 19 | Сесія двох вкладок | Авторизація активна | У вкладці А виконати вихід; перевірити вкладку Б | У вкладці Б стан узгоджено з відсутністю токена | |
| 20 | Mock API | VITE_USE_MOCK_API=true | Виконати ключові операції без бекенду | Дані відображаються з мок-сервісів | |
| 21 | Real API | Бекенд запущено, моки вимкнено | Виконати логін і один запит до захищеного ресурсу | Отримано очікувану відповідь або контрольовану помилку мережі | |
| 22 | Захист маршруту | Немає токена | Відкрити захищений URL | Перенаправлення на сторінку входу | |
| 23 | Продажі | Роль SalesManager | Відкрити /sales | Відображається панель продажів | |
| 24 | Навігація мобільна | Вузьке вікно браузера | Відкрити меню / док | Навігація доступна без втрати посилань | |
| 25 | Реєстр нестач | Administrator | Відкрити /brakimag | Відображено таблицю записів | |

*(Колонку «Статус» заповнюють під час виконання тестування: Pass / Fail / Blocked.)*

---

## Додаток Д  
### Матриця відповідності та ключові фрагменти коду

Додаток складається з двох частин: трасування функціональних вимог на тест-кейси та ілюстративні лістинги з клієнтської частини TrackTara (актуальність рядків звіряти з репозиторієм на момент здачі роботи). У тексті диплому доцільно оформити лістинги моноширинним шрифтом (наприклад, Courier New 10 pt) і підписувати «Лістинг Д.2.X».

---

### Додаток Д.1  
#### Матриця відповідності «функціональна вимога → тест-кейс»

**Таблиця Д.1 — Трасування вимог на тести**

| ID вимоги | Пов’язані тест-кейси (№ з табл. Г.1) | Коментар |
|-----------|--------------------------------------|----------|
| В.1 | 1, 2, 22 | Авторизація та негативний сценарій |
| В.2 | 4 | Залежить від налаштування OAuth та бекенду |
| В.3 | 6, 7 | Перевірка дозволених маршрутів |
| В.4 | 5 | Екран очікування ролі |
| В.5 | 6 | Доступ до hub |
| В.6 | 8, 9 | Основний та негативний сценарій видачі |
| В.7 | 10 | Запис нестачі |
| В.8 | 11, 12 | Підтвердження скану та додавання в коробку |
| В.9 | 13 | Переклад між коробками |
| В.10 | 14 | Створення замовлення |
| В.11 | 15, 16 | CRUD по тарі |
| В.12 | *(за окремим планом продуктів)* | Доповнити номерами кейсів каталогу |
| В.13 | 23 | Панель продажів |
| В.14 | 17 | Зміна ролей |
| В.15 | 25 | Перегляд реєстру нестач |
| В.16 | *(за наявності)* | Реєстр візків |
| В.17 | *(за наявності)* | Профіль |
| В.18 | 19 | Перезавантаження сторінки з авторизацією |
| В.19 | 20, 21 | Mock та реальний режими |
| В.20 | 2, 9 | Повідомлення користувачу |
| В.21 | 24 | Адаптив |
| В.22 | 22 | Захист без токена |
| В.23 | 18 | Вихід |
| В.24 | 19 | Поведінка між вкладками |

---

### Додаток Д.2  
#### Ключові фрагменти програмного коду

**Лістинг Д.2.1 — Конфігурація режиму mock/real API та базові URL (`src/utils/config/apiConfig.js`)**

```javascript
const useMockDefault =
  import.meta.env.DEV
    ? import.meta.env.VITE_USE_MOCK_API !== 'false'
    : import.meta.env.VITE_USE_MOCK_API === 'true';

export const API_CONFIG = {
  USE_MOCK_API: useMockDefault,

  BASE_URLS: {
    ACCOUNT: import.meta.env.VITE_API_ACCOUNT_URL || 'http://localhost:5081/account',
    USERS: import.meta.env.VITE_API_USERS_URL || 'http://localhost:5081/users',
    PRODUCTS: import.meta.env.VITE_API_PRODUCTS_URL || 'http://localhost:5081/products',
    CONTAINERS: import.meta.env.VITE_API_CONTAINERS_URL || 'http://localhost:5081/containers',
    CONTAINER_TYPES: import.meta.env.VITE_API_CONTAINER_TYPES_URL || 'http://localhost:5081/containers-type',
    PRODUCT_TYPES: import.meta.env.VITE_API_PRODUCT_TYPES_URL || 'http://localhost:5081/product-types',
    ROLES: import.meta.env.VITE_API_ROLES_URL || 'http://localhost:5081/roles',
    CATEGORIES: import.meta.env.VITE_API_CATEGORIES_URL || 'http://13.60.245.135:4312/categories',
    MANUFACTURERS: import.meta.env.VITE_API_MANUFACTURERS_URL || 'http://13.60.245.135:4312/manufacturers',
  },

  MOCK_DELAY: 500,
};
```

**Лістинг Д.2.2 — Фабрика сервісів: вибір реалізації mock або REST (`src/utils/services/ServiceFactory.js`)**

```javascript
const useMock = API_CONFIG.USE_MOCK_API;

export const AuthServiceInstance = useMock ? MockAuthService : AuthService;
export const UserServiceInstance = useMock ? MockUserService : UserService;
export const ProductServiceInstance = useMock ? MockProductService : ProductService;
export const ContainerServiceInstance = useMock ? MockContainerService : ContainerService;
export const OrderServiceInstance = useMock ? MockOrderService : OrderService;
export const ClientRouteServiceInstance = useMock ? MockClientRouteService : ClientRouteService;

export { AuthServiceInstance as AuthService };
export { UserServiceInstance as UserService };
```

*(У файлі також експортуються решта сервісів за тим самим принципом.)*

**Лістинг Д.2.3 — Захищений маршрут і перенаправлення користувача Guest (`src/routes/ProtectedRoute.jsx`)**

```javascript
const ProtectedRoute = ({ children, allowedRoles }) => {
  const token = localStorage.getItem("accessToken");
  let user = null;
  try {
    user = token ? jwtDecode(token) : null;
  } catch {
    user = null;
  }

  if (!token || !user) {
    return <Navigate to="/login" replace />;
  }

  const userRoles = getNormalizedRolesFromAccessToken(token);

  if (
    isAwaitingRoleAssignment(userRoles) &&
    !allowedRoles.includes(APP_ROLES.Guest)
  ) {
    return <Navigate to="/pending-role" replace />;
  }

  const isAuthorized = isRoleAllowed(userRoles, allowedRoles);

  return (
    <>
      {isAuthorized ? (
        children
      ) : (
        <ErrorMessage error="Немає доступу до цього розділу. Зверніться до адміністратора." />
      )}
    </>
  );
};
```

**Лістинг Д.2.4 — Нормалізація ролей із JWT та перевірка доступу (`src/utils/helpers/userRoles.js`, фрагмент)**

```javascript
export const APP_ROLES = {
  Administrator: "Administrator",
  Operator: "Operator",
  SalesManager: "SalesManager",
  User: "User",
  Guest: "Guest",
};

export function isAwaitingRoleAssignment(userRoles) {
  if (!Array.isArray(userRoles) || userRoles.length === 0) return false;
  const hasWorkRole = userRoles.some((r) =>
    [APP_ROLES.Operator, APP_ROLES.Administrator, APP_ROLES.SalesManager].includes(r)
  );
  return userRoles.includes(APP_ROLES.Guest) && !hasWorkRole;
}

export function isRoleAllowed(userRoles, allowedRoles) {
  if (!Array.isArray(userRoles) || !userRoles.length) return false;
  if (userRoles.includes(APP_ROLES.Administrator)) return true;
  return allowedRoles.some((r) => userRoles.includes(r));
}
```

**Лістинг Д.2.5 — Стартова маршрутизація за роллю Sales (`src/components/RoleHomeRedirect.jsx`)**

```javascript
const RoleHomeRedirect = () => {
  const roles = useAppRoles();
  const onlySales =
    roles.includes("SalesManager") &&
    !roles.includes("Operator") &&
    !roles.includes("Administrator");
  if (onlySales) return <Navigate to="/sales" replace />;
  return <WarehouseHub />;
};
```

**Лістинг Д.2.6 — Інтерцептор відповіді: повтор запиту після 401 (`src/utils/http/HttpClient.js`, фрагмент)**

```javascript
this.axiosInstance.interceptors.response.use(
  (response) => response,
  async (error) => {
    const originalRequest = error.config;

    if (error.response?.status === 401 && !originalRequest._retry) {
      originalRequest._retry = true;

      try {
        const token = await refreshToken(
          originalRequest,
          this.setAuthorizationToken.bind(this)
        );
        originalRequest.headers["Authorization"] = `Bearer ${token}`;
        return this.axiosInstance(originalRequest);
      } catch (refreshError) {
        return Promise.reject(refreshError);
      }
    }

    return Promise.reject(error);
  }
);
```

**Лістинг Д.2.7 — Оновлення токена та синхронізація Redux (`src/utils/http/tokenService.js`)**

```javascript
export const refreshToken = async (originalRequest, setAuthorizationToken) => {
  if (isRefreshing) {
    return new Promise((resolve, reject) => {
      failedQueue.push({ resolve, reject });
    }).then((token) => {
      if (token == null || token === "") {
        return Promise.reject(new Error("Refresh queue: empty token"));
      }
      originalRequest.headers["Authorization"] = `Bearer ${token}`;
      setAuthorizationToken(token);
      return token;
    });
  }

  isRefreshing = true;

  try {
    const refreshToken = localStorage.getItem("refreshToken");
    const accessToken = localStorage.getItem("accessToken");

    const { AuthService } = await import('../services');
    const tokens = await AuthService.refreshToken({ refreshToken, accessToken });

    if (tokens && tokens.accessToken) {
      localStorage.setItem("accessToken", tokens.accessToken);
      localStorage.setItem("refreshToken", tokens.refreshToken);

      setAuthorizationToken(tokens.accessToken);

      const decoded = jwtDecode(tokens.accessToken);
      const user = normalizeUserPayloadForAuth(decoded);
      store.dispatch(authUser(user));

      processQueue(null, tokens.accessToken);
      return tokens.accessToken;
    }
    throw new Error("Failed to refresh tokens");
  } catch (refreshError) {
    processQueue(refreshError, null);
    await logoutUser()(store.dispatch);
    return Promise.reject(refreshError);
  } finally {
    isRefreshing = false;
  }
};
```

**Лістинг Д.2.8 — Збереження стану Redux (`redux-persist`) (`src/store/store.js`, фрагмент)**

```javascript
const persistConfig = {
    key: 'tracktara',
    version: 1,
    storage,
    whitelist: [
        'user',
        'containerHistory',
        'productHistory',
        'role',
        'category',
        'manufacturer',
        'product',
        'appSettings',
        'users',
        'filters',
        'containers',
        'cartItem',
        'productTypes',
    ],
};

const persistedReducer = persistReducer(persistConfig, rootReducer);

export const store = configureStore({
    reducer: persistedReducer,
    middleware: (getDefaultMiddleware) =>
        getDefaultMiddleware({
            serializableCheck: {
                ignoredActions: [FLUSH, REHYDRATE, PAUSE, PERSIST, PURGE, REGISTER],
            },
        }).concat(thunk),
});

export const persistor = persistStore(store);
```

---

## Додаток Е  
### Глосарій термінів

**Таблиця Е.1 — Глосарій**

| Термін | Визначення |
|--------|------------|
| API | Програмний інтерфейс взаємодії між клієнтом і сервером за узгодженими правилами обміну даними. |
| Axios | Бібліотека для виконання HTTP-запитів із браузера або Node.js; у проєкті використовується для звернень до REST API. |
| JWT | Компактний формат представлення облікових зазначень між сторонами у вигляді підписаних JSON-токенів (access / refresh). |
| OAuth 2.0 | Рамковий протокол делегованої авторизації; Google OAuth використовується для зовнішнього входу користувача. |
| RBAC | Role-Based Access Control — керування доступом за ролями користувача до функцій і даних системи. |
| Redux | Бібліотека передбачуваного керування станом за патерном єдиного сховища та редюсерів. |
| Redux Toolkit | Офіційний набір інструментів для спрощення розробки на Redux у стилі «best practices». |
| redux-persist | Розширення для збереження частини Redux-стану у браузері між сеансами (наприклад, localStorage). |
| React | Бібліотека декларативної побудови користувацьких інтерфейсів на основі компонентів. |
| SPA | Single Page Application — веб-додаток, що завантажує один HTML-документ і динамічно оновлює вміст без повних перезавантажень сторінки. |
| React Router | Бібліотека маршрутизації для React-додатків із підтримкою вкладених маршрутів та захищених шляхів. |
| Vite | Засіб збірки та розробницького сервера з швидким холодним стартом та підтримкою ES-модулів. |
| Mock API | Імітація відповідей сервера на клієнті для демонстрації інтерфейсу без живого бекенду. |
| Контейнер (тара) | Об’єкт складського обліку для зберігання партії продукції з ідентифікатором та прив’язкою до сектора/ряду. |
| Замовлення | Документ або запис із переліком позицій до відбору та видачі на відвантаження. |
| Видача у візок | Операція перенесення кількості з позиції замовлення у транспортну одиницю (візок) на лінії складу. |
| Пакування в коробки | Формування відправних одиниць (коробок) з позицій, уже виданих у візок, з прив’язкою до замовлення та клієнта. |
| Реєстр нестач | Облік випадків нестачі товару з зазначенням кількості та причини для подальшого розслідування. |
| ER-модель | Entity–Relationship модель даних для опису сутностей та зв’язків між ними на концептуальному рівні. |
| UML | Unified Modeling Language — уніфікована мова графічного моделювання програмних систем. |
| Use case | Прецедент використання — сценарій взаємодії актора з системою для досягнення мети. |
| CORS | Cross-Origin Resource Sharing — механізм браузера для контролю доступу до ресурсів іншого походження (origin). |
| Environment variables | Змінні оточення конфігурації збірки та рантайму (префікс `VITE_` у проєкті Vite). |
| Hydration / Rehydrate | У контексті redux-persist — відновлення стану зі сховища при старті додатка. |

---

*Примітка для верстки диплому: замість текстових блоків `[МІСЦЕ ДЛЯ …]` підставити файли зображень (PNG або JPG), вирівнювання по центру сторінки, підпис до рисунку — безпосередньо під ілюстрацією; при необхідності масштаб зберегти читабельність шрифтів інтерфейсу.*
