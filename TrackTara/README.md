# 📦 TrackTara - Система управління контейнерами та продуктами

TrackTara - це веб-додаток для управління контейнерами та продуктами з підтримкою різних ролей користувачів та повним функціоналом CRUD операцій.

## 🚀 Швидкий старт

### Встановлення залежностей

```bash
npm install
```

### Налаштування

Створіть файл `.env` в корені проекту:

```env
VITE_USE_MOCK_API=true
```

### Запуск проекту

```bash
npm run dev
```

Проект буде доступний за адресою: `http://localhost:5173`

## 📋 Тестові облікові записи

### Адміністратор

- **Email:** `admin@test.com`
- **Password:** `password123`
- **Доступ:** Повний доступ до всіх функцій

### Оператор

- **Email:** `operator@test.com`
- **Password:** `password123`
- **Доступ:** Управління контейнерами та продуктами

## 🛠️ Технології

### Frontend

- **React 18** - UI бібліотека
- **Vite** - Збірщик та dev сервер
- **Redux Toolkit** - Управління станом
- **React Router v7** - Маршрутизація
- **Bootstrap 5** - UI компоненти
- **Material-UI** - Додаткові UI компоненти
- **Chart.js** - Візуалізація даних
- **React Toastify** - Сповіщення
- **Axios** - HTTP клієнт

### Додаткові бібліотеки

- **jwt-decode** - Декодування JWT токенів
- **react-select** - Компоненти вибору
- **yup** - Валідація форм
- **lodash** - Утиліти

## 📁 Структура проекту

```
TrackTara/
├── public/                          # Статичні файли
│   ├── Icons for functions/         # Іконки для функцій
│   └── images/                      # Зображення
├── src/
│   ├── components/                  # React компоненти
│   │   ├── common/                  # Загальні компоненти
│   │   │   ├── loader/              # Компонент завантаження
│   │   │   └── userMessage/         # Повідомлення користувачу
│   │   ├── layout/                  # Компоненти макету
│   │   │   ├── Header.jsx           # Шапка сайту
│   │   │   ├── Footer.jsx            # Підвал
│   │   │   ├── Layout.jsx           # Основний макет
│   │   │   └── HeadersLinks.jsx     # Навігаційні посилання
│   │   └── Sidebar.jsx               # Бічна панель
│   ├── hooks/                       # Custom React hooks
│   │   ├── useActions.js            # Хук для Redux actions
│   │   ├── useFilters.js            # Хук для фільтрів
│   │   ├── useDebouncedEffect.js    # Debounce ефект
│   │   ├── userImage.js             # Хук для зображень користувачів
│   │   └── productImage.js           # Хук для зображень продуктів
│   ├── pages/                       # Сторінки додатку
│   │   ├── auth/                    # Автентифікація
│   │   │   ├── login/                # Сторінка входу
│   │   │   ├── register/            # Сторінка реєстрації
│   │   │   └── google/              # Google OAuth
│   │   ├── home/                     # Головна сторінка
│   │   ├── products/                # Управління продуктами
│   │   │   └── components/
│   │   │       ├── ProductsList.jsx # Список продуктів
│   │   │       ├── ProductDetail.jsx # Деталі продукту
│   │   │       └── modals/          # Модальні вікна
│   │   ├── tare/                     # Управління контейнерами
│   │   │   └── components/
│   │   │       ├── ContainersTable.jsx # Таблиця контейнерів
│   │   │       ├── ContainerFilterForm.jsx # Фільтри
│   │   │       └── tareModals/      # Модальні вікна
│   │   ├── users/                    # Управління користувачами
│   │   ├── myProfile/                # Профіль користувача
│   │   ├── ProductTypesPage/         # Типи продуктів
│   │   └── containerTypes/           # Типи контейнерів
│   ├── router/                       # Маршрутизація
│   │   └── AppRouter.jsx
│   ├── routes/                       # Захищені маршрути
│   │   ├── BasicRoute.jsx            # Основні маршрути
│   │   └── ProtectedRoute.jsx        # Захищені маршрути
│   ├── store/                        # Redux store
│   │   ├── store.js                  # Конфігурація store
│   │   ├── actionCreator/           # Експорт всіх actions
│   │   └── state/
│   │       ├── actions/              # Redux actions
│   │       ├── reduserSlises/        # Redux slices
│   │       └── selectors/            # Redux selectors
│   └── utils/                        # Утиліти
│       ├── config/
│       │   └── apiConfig.js          # Конфігурація API
│       ├── http/
│       │   ├── HttpClient.js         # HTTP клієнт
│       │   └── tokenService.js       # Обробка токенів
│       └── services/                 # Сервіси
│           ├── ServiceFactory.js    # Фабрика сервісів
│           ├── index.js              # Центральний експорт
│           ├── mock/                 # Мок-сервіси
│           └── [реальні сервіси]     # Реальні сервіси
├── .env                              # Змінні оточення
├── package.json                      # Залежності
├── vite.config.js                    # Конфігурація Vite
└── README.md                          # Документація
```

## 🎯 Функціональність

### Автентифікація та авторизація

- ✅ Вхід/Реєстрація користувачів
- ✅ Google OAuth (підтримка)
- ✅ JWT токени з автоматичним оновленням
- ✅ Захищені маршрути на основі ролей
- ✅ Автоматична автентифікація при завантаженні

### Управління контейнерами

- ✅ CRUD операції з контейнерами
- ✅ Додавання/видалення продуктів з контейнерів
- ✅ Фільтрація контейнерів (за кодом, назвою, об'ємом, типом, станом)
- ✅ Сортування контейнерів
- ✅ Пагінація
- ✅ Детальна інформація про контейнер
- ✅ Історія змін контейнерів

### Управління продуктами

- ✅ CRUD операції з продуктами
- ✅ Детальна інформація про продукт
- ✅ Фільтрація та пошук продуктів
- ✅ Улюблені продукти (для ролі User)

### Управління користувачами (тільки Administrator)

- ✅ Перегляд списку користувачів
- ✅ Створення нових користувачів
- ✅ Видалення користувачів
- ✅ Зміна ролей користувачів

### Управління типами (тільки Administrator)

- ✅ Типи продуктів - CRUD операції
- ✅ Типи контейнерів - CRUD операції

### Профіль користувача

- ✅ Перегляд профілю
- ✅ Редагування даних
- ✅ Завантаження зображення профілю

### Додаткові функції

- ✅ Глобальний loader для API запитів
- ✅ Toast сповіщення
- ✅ Валідація форм
- ✅ Адаптивний дизайн
- ✅ Графіки та статистика (на головній сторінці)

## 🔐 Ролі та права доступу

### Administrator (Адміністратор)

Повний доступ до всіх функцій:

- Управління користувачами
- Управління типами продуктів та контейнерів
- Управління контейнерами
- Управління продуктами
- Профіль

### Operator (Оператор)

Доступ до операційних функцій:

- Управління контейнерами
- Управління продуктами
- Профіль

### User (Користувач)

Обмежений доступ (якщо реалізовано):

- Перегляд продуктів
- Улюблені продукти
- Кошик

## 🔧 Mock API

Проект використовує систему Mock API для розробки без залежності від бекенду.

### Перемикання між моками та реальним API

В файлі `.env`:

```env
# Для мок-даних
VITE_USE_MOCK_API=true

# Для реального API (коли буде готовий)
# VITE_USE_MOCK_API=false
```

### Мок-дані

Моки містять початкові дані:

- 3 тестових користувачі
- 3 продукти
- 3 контейнери
- 4 типи продуктів
- 4 типи контейнерів

Дані зберігаються в пам'яті та скидаються при перезавантаженні сторінки.

**Детальна інформація:** Дивіться [MOCK_API_GUIDE.md](./MOCK_API_GUIDE.md)

## 📝 Команди

```bash
# Запуск dev сервера
npm run dev

# Збірка для продакшну
npm run build

# Перегляд збірки
npm run preview

# Перевірка коду (lint)
npm run lint
```

## 🏗️ Архітектура

### State Management (Redux)

- **Redux Toolkit** для управління станом
- **Redux Thunk** для асинхронних actions
- Розділення на slices за доменами:
  - `userSlice` - поточний користувач
  - `usersSlice` - список користувачів
  - `productSlice` - продукти
  - `containerSlice` - контейнери
  - `containerTypeSlice` - типи контейнерів
  - `productTypeSlice` - типи продуктів
  - `appSettingSlice` - глобальні налаштування
  - та інші...

### Маршрутизація

- **React Router v7** для навігації
- Захищені маршрути через `ProtectedRoute`
- Перевірка ролей для доступу до сторінок

### Сервіси

- **ServiceFactory** автоматично вибирає між моками та реальним API
- Всі сервіси мають однакові інтерфейси
- Централізований експорт через `utils/services/index.js`

### HTTP клієнт

- **HttpClient** - обгортка над Axios
- Автоматичне додавання токенів
- Interceptor для оновлення токенів
- Глобальний loading state

## 🎨 UI/UX

### Компоненти

- **Bootstrap 5** для основного UI
- **Material-UI** для іконок та додаткових компонентів
- **React Select** для вибору з пошуком
- **Chart.js** для графіків

### Адаптивність

- Мобільна версія з Offcanvas для фільтрів
- Адаптивні таблиці
- Responsive навігація

## 📚 Документація

- [MOCK_API_GUIDE.md](./MOCK_API_GUIDE.md) - Гайд по використанню Mock API
- [MIGRATION_CHECKLIST.md](./MIGRATION_CHECKLIST.md) - Чеклист міграції
- [ARCHITECTURE_DECISION.md](./ARCHITECTURE_DECISION.md) - Архітектурні рішення

## 🐛 Troubleshooting

### Помилки при запуску

1. **Помилка з імпортами сервісів**

   - Перевірте що використовуєте `import { Service } from '@/utils/services'`
   - Не використовуйте прямі імпорти з `services/ServiceName`

2. **Моки не працюють**

   - Перевірте наявність `.env` файлу
   - Перевірте що `VITE_USE_MOCK_API=true`
   - Перезапустіть dev сервер

3. **Помилки з токенами**
   - Очистіть localStorage: `localStorage.clear()`
   - Спробуйте увійти знову

### Консоль браузера

При успішному запуску з моками ви побачите:

```
🔧 [SERVICE FACTORY] Using MOCK services
```

## 🔄 Майбутні покращення

- [ ] Персистентність мок-даних (localStorage)
- [ ] Додаткові тестові дані
- [ ] Покращена обробка помилок
- [ ] Unit тести
- [ ] E2E тести

## 📄 Ліцензія

Приватний проект

## 👤 Автор

TrackTara Development Team

---

**Примітка:** Проект використовує Mock API для незалежної розробки фронтенду. Всі реальні сервіси збережені та готові до використання коли бекенд буде готовий.
